# Generated by the POC generator ({{stack}}) — review before committing
FROM python:{{pythonVersion}}-slim
WORKDIR /app
RUN addgroup --system app && adduser --system --ingroup app app
COPY requirements.txt* pyproject.toml* ./
RUN pip install --no-cache-dir {{installCmd}} && pip install --no-cache-dir streamlit
COPY . .
USER app
ENV PORT=8080
EXPOSE 8080
CMD ["sh", "-c", "streamlit run {{entryScript}} --server.port ${PORT} --server.address 0.0.0.0"]
