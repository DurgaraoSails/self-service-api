server {
    listen       ${PORT};
    server_name  _;

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 'ok';
    }

    root   /usr/share/nginx/html;
    index  index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
