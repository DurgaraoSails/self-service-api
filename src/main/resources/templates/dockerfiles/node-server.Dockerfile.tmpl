# Generated by the POC generator ({{stack}}) — review before committing
# Assumes the app reads its port from the PORT environment variable — review this against your
# own server startup code before committing.
FROM node:{{nodeVersion}}-slim AS build
WORKDIR /app
COPY package.json package-lock.json* yarn.lock* pnpm-lock.yaml* ./
RUN {{installCmd}}
COPY . .
RUN npm run build --if-present

FROM node:{{nodeVersion}}-slim
WORKDIR /app
ENV NODE_ENV=production
COPY --from=build /app ./
USER node
ENV PORT=8080
EXPOSE 8080
CMD ["sh", "-c", "{{startCmd}}"]
