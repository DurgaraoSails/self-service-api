# Generated by the POC generator ({{stack}}) — review before committing
FROM mcr.microsoft.com/dotnet/sdk:{{dotnetVersion}} AS build
WORKDIR /app
COPY . .
RUN dotnet publish -c Release -o /app/publish

FROM mcr.microsoft.com/dotnet/aspnet:{{dotnetVersion}}
WORKDIR /app
COPY --from=build /app/publish .
USER $APP_UID
ENV PORT=8080
EXPOSE 8080
ENTRYPOINT ["sh", "-c", "ASPNETCORE_URLS=http://0.0.0.0:${PORT} dotnet {{assemblyName}}.dll"]
