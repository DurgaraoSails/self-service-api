# Generated by the POC generator ({{stack}}) — review before committing
FROM golang:{{goVersion}} AS build
WORKDIR /app
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server {{mainPackage}}

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /app/server /server
ENV PORT=8080
EXPOSE 8080
ENTRYPOINT ["/server"]
