spring:
  application:
    name: self-service-api

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/selfservice}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:admin}

    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${SMTP_AUTH:false}
          starttls:
            enable: ${SMTP_STARTTLS:false}

auth:
  dev-token-issuance:
    # POST /auth/tokens — off everywhere by default, including local dev. Set true only in a
    # personal, never-committed config (e.g. application-local.yaml) when you need it for manual
    # testing; never set it in any deployable base config.
    enabled: ${AUTH_DEV_TOKEN_ISSUANCE_ENABLED:false}

otp:
  hash-secret: ${OTP_HASH_SECRET:local-dev-otp-secret-change-me}
  expiry-minutes: 10
  max-attempts: 5
  max-resends: 3
  max-requests-per-hour: 5

registration-verification:
  ttl-hours: ${REGISTRATION_VERIFICATION_TTL_HOURS:24}

jwt:
  issuer: ${JWT_ISSUER:self-service-api}
  private-key-path: ${JWT_PRIVATE_KEY_PATH}
  public-key-path: ${JWT_PUBLIC_KEY_PATH}
  access-token-ttl: ${JWT_ACCESS_TOKEN_TTL:30m}
  refresh-token-ttl: ${JWT_REFRESH_TOKEN_TTL:7d}

  # POC-scoped tokens (POST /pocs/{slug}/launch). Deliberately far shorter than an access token:
  # this one lives in JavaScript on a POC's own origin, and the portal is the only thing that can
  # refresh it — which is what makes signing out of the portal end POC access within one TTL,
  # with no revocation infrastructure anywhere.
  poc-token-ttl: ${JWT_POC_TOKEN_TTL:15m}
  # Identifies which signing key issued a token (JWT `kid` header / JWKS `kid`). Bump this
  # alongside rotating the RSA key pair so old and new tokens verify against the right entry.
  key-id: ${JWT_KEY_ID:1}
  # Extra public keys published in JWKS for verification only — see JwtProperties for the
  # rotation procedure this enables. Empty by default (today's single-key behavior). Set via
  # JWT_ADDITIONAL_PUBLIC_KEY_PATHS as a comma-separated list, e.g. "file:./secrets/jwt/next-public-key.pem".
  additional-public-key-paths: ${JWT_ADDITIONAL_PUBLIC_KEY_PATHS:}

trial:
  length-days: ${TRIAL_LENGTH_DAYS:14}

files:
  # 'gcs' is production. 'local' writes under a directory on this machine instead, so the feature
  # runs with no GCP credentials at all — the same reasoning as pipeline.executor=skip. Never
  # durable in a deploy: Cloud Run's filesystem is memory-backed and per-instance.
  storage: ${FILE_STORAGE:local}
  # gcs only. Its own bucket, not a prefix in the pipeline's — retention and access are this
  # bucket's whole purpose. Blank with storage=gcs fails at startup rather than at first upload.
  bucket: ${FILE_STORAGE_BUCKET:}
  # local only. Blank uses <system temp>/self-service-files.
  local-dir: ${FILE_STORAGE_LOCAL_DIR:}

  # Raised from the 5 MB first specced: 5 MB rejects most *scanned* documents (10-20 MB), which is
  # what a prospect actually uploads to a contract-review demo. Still under a third of Cloud Run's
  # 32 MiB request ceiling — the headroom that lets the multipart spool threshold sit above this
  # limit, so an upload never touches the (memory-backed) filesystem. Changing this means changing
  # the spool threshold and the container's memory together, not on its own.
  max-file-size: ${FILE_MAX_FILE_SIZE:10MB}
  max-files-per-poc: ${FILE_MAX_FILES_PER_POC:20}
  max-bytes-per-user: ${FILE_MAX_BYTES_PER_USER:250MB}

  # Selects from the types ContentTypeValidator has a content signature for. Adding one that has
  # no signature fails startup: skipping validation for it would quietly turn this allowlist back
  # into a claim from the client, which is the thing it exists to replace.
  allowed-content-types:
    - application/pdf
    - application/vnd.openxmlformats-officedocument.wordprocessingml.document
    - application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
    - application/vnd.openxmlformats-officedocument.presentationml.presentation
    - text/plain
    - text/csv
    - image/png
    - image/jpeg

admin:
  emails: ${ADMIN_EMAILS:}

email-domain-validation:
  timeout-millis: ${EMAIL_DOMAIN_VALIDATION_TIMEOUT_MILLIS:2000}
  retries: ${EMAIL_DOMAIN_VALIDATION_RETRIES:1}

support:
  sales-email: ${SALES_EMAIL:no.reply.sails.poc@gmail.com}

deployment:
  # 'in-process' runs the real pipeline asynchronously in this JVM. 'logging' is a stub that
  # only logs, letting you drive status transitions by hand via POST .../status.
  trigger: ${DEPLOYMENT_TRIGGER:in-process}
  # Unused by the in-process trigger; kept for the (still-wired, still-working) manual status
  # callback and the source-repository/upstream-commit endpoints, none of which this needs.
  webhook-secret: ${DEPLOYMENT_WEBHOOK_SECRET:local-dev-deployment-webhook-secret-change-me}

gcp:
  project-id: ${GCP_PROJECT_ID:}
  region: ${GCP_REGION:}
  environment: ${GCP_ENVIRONMENT:dev}

pipeline:
  # 'cloud-build' submits every build and deploy to Cloud Build; 'skip' does none of it and reports
  # each request SKIPPED, for running the app with no GCP account at all. Unset is also skip (the
  # skipping executor claims that case, and isSkip() agrees with it). Nothing else is accepted — a
  # leftover 'local' (that executor was removed), or a present-but-empty value, fails at startup
  # rather than silently skipping.
  executor: ${PIPELINE_EXECUTOR:skip}

  # This app's own GitHub credential, for REST API calls only: read a branch head, read poc.yaml,
  # create the release tag. Needed even for a public repo, because tagging is a write. It is never
  # sent to Cloud Build — the clone inside a build reads the same secret from Secret Manager
  # itself, so no literal token is ever stored on a Build resource. Supplied like any other
  # credential here: this env var in a deployed environment, secrets/application-local-secrets.yaml
  # locally.
  github-token: ${GITHUB_TOKEN:}

  # Identity the build runs as. Required under cloud-build, and must be the account holding
  # secretAccessor on the github-token secret (self-service-builder, per self-service-terraform):
  # blank would run builds as Cloud Build's own default service account, which holds no such grant,
  # and every clone would fail. Blank is therefore refused at startup under cloud-build; it stays
  # the default here only because 'skip' submits no builds and needs no account.
  build-service-account: ${BUILD_SERVICE_ACCOUNT:}
  grant-api-invoker: ${BUILD_GRANT_API_INVOKER:false}
  allow-unauthenticated: ${BUILD_ALLOW_UNAUTHENTICATED:true}

  build-timeout: ${PIPELINE_BUILD_TIMEOUT:20m}
  build-poll-interval: ${PIPELINE_BUILD_POLL_INTERVAL:10s}

  # Platform-wide fallback for the branch a new version is cut from. A POC's own deployBranch
  # (set in the admin form) wins over this; this covers the POCs that name no branch, and blank —
  # the default — leaves those following each repository's own default branch. Set it only when
  # every unconfigured POC must release from one branch name regardless of its repository.
  deploy-branch: ${PIPELINE_DEPLOY_BRANCH:}

# What a deployed POC may rely on about the container it runs in. Separate from pipeline: above,
# which describes how the deploy is carried out rather than what it produces. Every value here is
# injected into every container and reserved from poc.yaml (see manifest.reserved-env-names).
poc-runtime:
  ingress-port: ${POC_RUNTIME_INGRESS_PORT:8080}
  platform-api-url: ${SELF_SERVICE_API_URL:http://localhost:8080}
  # The one origin allowed to frame a deployed POC — injected as PORTAL_ORIGIN, used both as the
  # postMessage targetOrigin a POC replies to and in its own Content-Security-Policy:
  # frame-ancestors, so the script-level origin check and the browser-enforced one cannot disagree.
  # Defaults to the portal URL this API already configures for outbound email links.
  portal-origin: ${POC_RUNTIME_PORTAL_ORIGIN:${app.frontend.url}}

manifest:
  # Env vars a container's poc.yaml entry may never set — the platform owns these directly.
  reserved-env-names: ${MANIFEST_RESERVED_ENV_NAMES:PORT,POC_SLUG,PLATFORM_API_URL,PORTAL_ORIGIN}
  # Env var name prefixes a container's poc.yaml entry may never set — reserved for
  # platform-injected values (e.g. SVC_<NAME>_URL for reaching a sidecar by name).
  reserved-env-prefixes: ${MANIFEST_RESERVED_ENV_PREFIXES:SAILS_,SVC_}
  # Cloud Run's own per-service container limit, checked here rather than surfacing later as a
  # gcloud error partway through a deploy.
  max-containers: ${MANIFEST_MAX_CONTAINERS:8}

app:
  mail:
    from-address: ${MAIL_FROM_ADDRESS}
    from-name: ${MAIL_FROM_NAME:Self Service}
  frontend:
    url: ${FRONTEND_URL:http://localhost:4200}

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
  endpoint:
    health:
      probes:
        enabled: true

server:
  # Bind all interfaces, not just loopback — a container's port is unreachable otherwise.
  address: 0.0.0.0
  port: ${PORT:8080}

brevo:
  api-key: ${BREVO_API_KEY}
